Step 4 implemented the SQLite-backed index and `asm reindex` CLI path only: `cargo build` succeeds, `cargo test` passes all 5 integration checks including the 4 existing parser tests plus `tests/index.rs` round-tripping every `Session` field from a fixture through `Index::upsert_session()` / `list_all()`. Real reindex validation against `~/.claude` and `~/.codex` completed with first-run `ReindexStats: discovered=1559 parsed=1558 skipped_unchanged=0 failed=1` and second-run `ReindexStats: discovered=1559 parsed=1 skipped_unchanged=1557 failed=1`, so the second run skipped 99%+ of discovered files; the one repeated parser failure was a Claude file with no parseable timestamped event, and the one second-run parsed file was an actively changing session file during this Codex run. No TUI/list/resume/fork/liveness/FTS implementation was added beyond placeholder CLI messages for later steps.
