cargo test: 59 passed; 0 failed.

Inline chip badges now render inside the search input before typed text, and chip deletion requires two plain Backspace taps at cursor start: first highlights the rightmost chip, second deletes it while Alt+Backspace only deletes composer text words.
