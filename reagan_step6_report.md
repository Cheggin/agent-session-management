Step 6 implemented terminal-safe resume dispatch and Step 5.5 pre-filter seeding: Enter now clones the selected `Session`, explicitly restores the TUI terminal state before handoff, then dispatches `Agent::Claude` as `claude --resume <session.id>` and `Agent::Codex` as `codex resume <session.id>` via Unix `exec()` with non-Unix spawn/wait fallback; an empty selected session id prints an error and exits 1. The `--filter <expr>` flag is supported for bare `asm` and `asm ls`, tokenizes on whitespace, feeds each token through the existing chip grammar with an added `@`, exits 2 on invalid tokens before TUI startup, and seeds chips into the app with empty composer search text while preserving backspace removal at cursor start. Validation passed: `cargo build --release` finished successfully (`Finished release profile [optimized] target(s) in 1.14s`), `cargo test` passed all suites (`tests/cli.rs`: 3 passed; `tests/filter.rs`: 6 passed; `tests/index.rs`: 1 passed; `tests/parser.rs`: 4 passed; doc tests: 0; total integration tests: 14 passed, with the 11 prior tests still green), and `cargo clippy --all-targets -- -D warnings` passed; the new pre-filter parse tests cover `claude -> [Agent(Claude)]`, `branch:main path:foo -> [Branch("main"), PathSubstring("foo")]`, and `notathing -> Err`. No fork/export/liveness/FTS/install work was touched; the only additional Step 6 wiring was making the existing `asm resume <id>` placeholder use the same provider-agnostic indexed-session dispatch path.
